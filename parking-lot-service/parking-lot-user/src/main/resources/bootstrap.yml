server:
  port: 9092
spring:
  application:
    name: service-user
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 192.168.6.6:8848
        # 共享配置
        shared-configs[0]:
          dataId: common-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
          refresh: true
        shared-configs[1]:
          dataId: service-common-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
          refresh: true
      discovery:
        server-addr: 192.168.6.6:8848
